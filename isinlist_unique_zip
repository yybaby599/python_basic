如果你要判断 df['name'] 这一列的值是不是在 mylist 里，就这样写：

res = df[df['name'].isin(mylist)]
print(res)
.isin() 等操作能接受 Series。
如果不在意顺序，可以直接用 set：

lst = [1,2,2,3]
unique = list(set(lst))   # [1,2,3]，但顺序可能打乱

insurance['location'] =list(zip(insurance['lat'], insurance['lon']))

如果你确实要 list，可以：

start_ids.tolist()
